version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dilkatelephone-web
    restart: always
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
    # Add other environment variables here from .env if needed
    # env_file:
    #   - .env
